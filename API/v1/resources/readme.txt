'Resources' sub directory contains PHP files which define Resources.

Resources has four CRUD Methods(POST, GET, PUT, DELETE). and Each HTTP Methods have its own buisiness logic as form of closure which returns Response code and message and JSON formed data if it has.

The Files in this directory will automatically be included in your API.

be careful to syntex error.