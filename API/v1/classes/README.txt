'classes' sub-directory contains the claases the framework provides.

the classes in this folder are very import, define core functionality of the framework.

all PHP files in classes sub-directory had been included in default.