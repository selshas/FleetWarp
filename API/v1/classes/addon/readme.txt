'/classes/addon' sub-directory contains the classes which is define extra features the framework provide.

these classes are not essential. It might be included where you want, when you need it.

also you can ignore them for performance when you don't need.